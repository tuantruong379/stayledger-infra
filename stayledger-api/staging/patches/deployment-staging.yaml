# Staging-specific deployment overrides (CORS, version label).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stayledger-api
spec:
  template:
    metadata:
      labels:
        # Align pod version label with deployed API image tag f1b2e27
        version: "f1b2e27"
    spec:
      containers:
        - name: api
          env:
            - name: ALLOWED_ORIGINS
              value: "https://stg-app.stayledger.io,http://10.89.1.40:30010"
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
              ephemeral-storage: "50Mi"
            limits:
              cpu: "2"
              memory: "1Gi"
              ephemeral-storage: "200Mi"
