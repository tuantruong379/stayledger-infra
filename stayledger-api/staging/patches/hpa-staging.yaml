# P1C — enable API autoscaling on staging after connection_limit budget is sized.
# See docs/runbooks/p1c-hpa-capacity-apply.md
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: stayledger-api
spec:
  minReplicas: 2
  maxReplicas: 6
