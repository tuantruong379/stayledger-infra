apiVersion: apps/v1
kind: Deployment
metadata:
  name: stayledger-ai-worker
spec:
  replicas: 2
  template:
    metadata:
      labels:
        # Align AI worker version label with API image tag e40d7de
        version: "e40d7de"
