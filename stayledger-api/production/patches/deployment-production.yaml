apiVersion: apps/v1
kind: Deployment
metadata:
  name: stayledger-api
spec:
  # Steady-state 1 (RWO guest-docs). RollingUpdate maxUnavailable:0 + maxSurge:1
  # (from base) keeps the old Ready pod serving until the surge pod is Ready.
  replicas: 1
  minReadySeconds: 15
  # Best practice: cap rollback history in production (was unset -> live drifted to 3 via ad-hoc patch).
  revisionHistoryLimit: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        # Match kustomization images pin (tag 192294f / release/1.4.9).
        version: "192294f"
      annotations:
        stayledger.io/git-sha: "192294f"
        stayledger.io/branch: "release/1.4.9"
        kubernetes.io/change-cause: "Infra cleanup — CORS/FRONTEND_URL via ConfigMap; version sync 192294f"
    spec:
      containers:
        - name: api
          # Single replica (RWO guest-docs) — give this pod more CPU than landing.
          # Request 350m / limit 1 CPU so API wins scheduling vs marketing site.
          # ALLOWED_ORIGINS + FRONTEND_URL come from stayledger-api-config (envFrom).
          resources:
            requests:
              cpu: "350m"
              memory: "768Mi"
            limits:
              cpu: "1000m"
              memory: "1536Mi"
