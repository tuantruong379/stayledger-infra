# Namespace for AI Hotel Assistant API + in-cluster Redis (on-prem K8s).
apiVersion: v1
kind: Namespace
metadata:
  name: hotel-assistant
  labels:
    app.kubernetes.io/part-of: hotel-assistant
    # Enable Prometheus scraping for ServiceMonitor discovery.
    monitoring: "true"
