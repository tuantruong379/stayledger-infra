apiVersion: v1
kind: PersistentVolume
metadata:
  name: postgres-backup-pv
spec:
  local:
    path: /mnt/data/stayledger/ai-assistant/postgres-backup
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - stayledger
