# Runtime API origin for Next.js (replaced at container start via docker-entrypoint.sh).
apiVersion: v1
kind: ConfigMap
metadata:
  name: hotel-assistant-frontend-config
  namespace: hotel-assistant
data:
  NEXT_PUBLIC_API_BASE_URL: "https://api-assistant.stayledger.io"
