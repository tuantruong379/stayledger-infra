# Deprecated: the image entrypoint owns runtime URL injection. Do not add a
# second sed-based bundle rewrite here; it can expose the in-cluster hostname to
# browser JavaScript. Kept as a local note for old overlays; staging no longer
# references this patch.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stayledger-ai-assistant-admin-web
  namespace: stayledger-ai-assistant
spec:
  template:
    spec:
