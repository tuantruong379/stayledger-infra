apiVersion: apps/v1
kind: Deployment
metadata:
  name: stayledger-admin-web
spec:
  replicas: 2
  template:
    metadata:
      labels:
        # Align pod version label with deployed admin-web image tag
        version: "staging-20260720a"
