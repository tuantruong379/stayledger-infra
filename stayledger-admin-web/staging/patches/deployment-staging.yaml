apiVersion: apps/v1
kind: Deployment
metadata:
  name: stayledger-admin-web
spec:
  replicas: 2
  # Cap stale ReplicaSet history (Cycle 10 staging hygiene).
  revisionHistoryLimit: 3
  template:
    metadata:
      labels:
        # Cycle 10 a11y + residual + cachebust candidate 4066ee7
        version: "staging-4066ee7"
