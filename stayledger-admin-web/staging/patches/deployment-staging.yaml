apiVersion: apps/v1
kind: Deployment
metadata:
  name: stayledger-admin-web
spec:
  # Single-node hub: 2nd replica is unschedulable under DoNotSchedule topology.
  replicas: 1
  # Cap stale ReplicaSet history (Cycle 10 staging hygiene).
  revisionHistoryLimit: 3
  template:
    metadata:
      labels:
        # P1-P2 follow-up: permission-limited titles + GM dashboard gate
        version: "staging-6b8d3a6"


