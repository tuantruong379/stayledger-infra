apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-workers-egress-nats
  namespace: stayledger-ai-assistant
spec:
  podSelector:
    matchExpressions:
      - key: app.kubernetes.io/name
        operator: In
        values:
          - stayledger-ai-assistant-channel-worker
          - stayledger-ai-assistant-webhook-worker
  policyTypes:
    - Egress
  egress:
    - to:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: stayledger-ai-assistant-nats
      ports:
        - protocol: TCP
          port: 4222
