# Non-sensitive SMTP settings (host, port, From identity, TLS behavior).
# Credentials belong in Secret ``stayledger-ai-assistant-smtp-secrets`` (see example file).
apiVersion: v1
kind: ConfigMap
metadata:
  name: stayledger-ai-assistant-smtp-config
  namespace: stayledger-ai-assistant
  labels:
    app.kubernetes.io/name: stayledger-ai-assistant-api
    app.kubernetes.io/part-of: stayledger-ai-assistant
    app.kubernetes.io/component: smtp
data:
  SMTP_HOST: "email-smtp.ap-southeast-1.amazonaws.com"
  SMTP_PORT: "587"
  SMTP_FROM_EMAIL: "no-reply@stayledger.io"
  SMTP_FROM_NAME: "StayLedger"
  # Fallback ops recipient when a tenant has no tenant_runtime_config.notification_emails
  # and no legacy tenant routing entry. Comma-separate multiple addresses.
  SMTP_OPS_TO_EMAIL: "tuan.truong@flownexa.ai"
  SMTP_TIMEOUT_S: "10"
  SMTP_MAX_RETRIES: "2"
  SMTP_RETRY_BACKOFF_S: "1.0"
  SMTP_USE_TLS: "false"
  SMTP_STARTTLS: "true"
  SMTP_VALIDATE_CERTS: "true"
  SMTP_REQUIRE_AUTH: "true"
